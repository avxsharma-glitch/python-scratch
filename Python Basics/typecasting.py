a = "1"
b = "2"
print( a+b) # This will concatenate the two strings and print "12"


x = 1 
y = 2
print( x+y) # This will add the two integers and print 3

#The conversion of one data type to another is called typecasting. In Python, we can use the built-in functions int(), float(), str() to perform typecasting.

print( int(a) + int(b)) # This will convert the strings to integers and then add them, printing 3

# implicit typecasting 
c = 5
d = 2.5
print(type(c)) # This will print <class 'int'>
print(type(d)) # This will print <class 'float'>
print( c + d) # This will implicitly convert the integer to a float and then add
# explicgitly converting a string to an integer
a = "1"
b = "2"
print( a+b) # This will concatenate the two strings and print "12"


x = 1 
y = 2
print( x+y) # This will add the two integers and print 3

#The conversion of one data type to another is called typecasting. In Python, we can use the built-in functions int(), float(), str() to perform typecasting.

print( int(a) + int(b)) # This will convert the strings to integers and then add them, printing 3

# implicit typecasting 
c = 5
d = 2.5
print(type(c)) # This will print <class 'int'>
print(type(d)) # This will print <class 'float'>
print( c + d) # This will implicitly convert the integer to a float and then add
# explicgitly converting a string to an integer
